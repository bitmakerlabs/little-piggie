<!doctype html>
<html>
  <head>
    <title>You fancy now!</title>
    <style>
      html, body {
        height: 100%;
        position: relative;
      }
      .container {
        font-family: sans-serif;
        font-size: 3em;
        line-height: 1em;
        position: absolute;
        top: 40%;
        width: 100%;
      }

      .container > p {
        width: 50%;
        margin: 0 auto;
      }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>